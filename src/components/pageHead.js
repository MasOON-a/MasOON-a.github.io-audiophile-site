import React from 'react';
// This is just the black bar at the top of the screen that serves as a background
// for the nav

export default function PageHead () {
    return(
        <div className="header"></div>
    )
}
